$ ollam serve
$ sudo apt install virtualenv
$ virtualenv ~/penv3
$ source ~/penv3/bin/activate
$ pip install langchain
$ pip install -qU langchain-ollama
$ pip install chromadb
$ pip install pypdf
$ pip install pytest
